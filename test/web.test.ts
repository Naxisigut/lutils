import { test, expect } from 'vitest';
import { getWebCookie, copyToClipboard } from '../src/web/index';

test('getWebCookie', () => {
  // expect(getWebCookie('abc')).toBe(undefined)   
})

test('copyToClipboard', () => {
  // expect(copyToClipboard('abc')).toBe(undefined)   
})